#include <stdio.h>
#include <string.h>
int main()
{
	char str[255];
	int size = 0, start =0, end =0, mid = 0;
	printf("Enter the string\n");
	scanf("%s", str);

	size = strlen(str);

	printf("-->%s %d\n",str, size);

        start = 0;
	end = size - 1;
	mid = start + (end -start) / 2;
	printf("-->%s %d %d\n",str, size, mid);
	
	
	
	find_palindrome(






12345678910
forgeeksskeegfor


strt = 0;
end = length -1;

start+1;
start--
end--







}
